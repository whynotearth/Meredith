# https://docs.microsoft.com/azure/devops/pipelines/yaml-schema

trigger:
- azure
- master
- release/*
pr: none

variables:
- name: DOTNET_SKIP_FIRST_TIME_EXPERIENCE
  value: true

pool:
  vmImage: 'windows-latest'

steps:
- task: DotNetCoreCLI@2
  displayName: 'Build'
  inputs:
    command: build
    arguments: '--configuration Release'

- task: DotNetCoreCLI@2
  displayName: 'Test'
  inputs:
    command: test
    projects: '*Tests/*.csproj'
    arguments: '--configuration Release'

- task: DotNetCoreCLI@2
  inputs:
    command: publish
    publishWebProjects: true
    arguments: '--configuration Release --output publish'
    zipAfterPublish: false

- task: OctopusPack@4
  inputs:
    PackageId: 'WhyNotEarth.Meredith.App'
    PackageFormat: 'NuPkg'
    PackageVersion: '0.10.0.$(Build.BuildId)'
    SourcePath: 'publish'
    OutputPath: 'OctopusPackages'

- task: NuGetCommand@2
  displayName: 'Push NuGet package to Octopus feed'
  inputs:
    command: push
    packagesToPush: 'OctopusPackages/*.nupkg'
    nuGetFeedType: 'internal'
    publishVstsFeed: 'meredith-core/OctopusFeed'

- task: OctopusCreateRelease@3
  displayName: 'Create Octopus Release'
  inputs:
    OctoConnectedServiceName: 'OctopusServer'
    ProjectName: 'Meredith'
    ReleaseNumber: '0.10.0.$(Build.BuildId)'
    ${{ if eq(variables['Build.SourceBranchName'], 'master') }}:
      Channel: 'production'
    ${{ if ne(variables['Build.SourceBranchName'], 'master') }}:
      Channel: 'Default'
    AdditionalArguments: "--packageversion=0.10.0.$(Build.BuildId)"